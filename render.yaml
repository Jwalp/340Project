services:
  - type: web
    name: fileverse
    env: node
    region: oregon
    plan: free
    buildCommand: npm run install:all && npm run build:frontend
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
